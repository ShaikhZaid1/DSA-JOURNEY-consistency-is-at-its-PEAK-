01_Extract_Digits.cpp
#include <iostream>
using namespace std;

int main()
{
    int n;
    cin >> n;

    while (n > 0)
    {
        int lastDigit = n % 10;
        cout << lastDigit << " ";
        n = n / 10;
    }

    return 0;
}
02_Count_Digits.cpp
#include <iostream>
using namespace std;

int main()
{
    int n;
    cin >> n;

    int count = 0;

    while (n > 0)
    {
        int lastDigit = n % 10;
        n = n / 10;
        count++;
    }

    cout << count;

    return 0;
}
03_Reverse_Number.cpp
#include <iostream>
using namespace std;

int main()
{
    int n;
    cin >> n;

    int revNum = 0;

    while (n > 0)
    {
        int lastDigit = n % 10;
        n = n / 10;
        revNum = (revNum * 10) + lastDigit;
    }

    cout << revNum;

    return 0;
}
04_Palindrome_Number.cpp
#include <iostream>
using namespace std;

int main()
{
    int n;
    cin >> n;

    int original = n;
    int revNum = 0;

    while (n > 0)
    {
        int lastDigit = n % 10;
        n = n / 10;
        revNum = (revNum * 10) + lastDigit;
    }

    if (original == revNum)
        cout << "Palindrome";
    else
        cout << "Not Palindrome";

    return 0;
}
05_Armstrong_Number.cpp
#include <iostream>
using namespace std;

int main()
{
    int n;
    cin >> n;

    int original = n;
    int sum = 0;

    while (n > 0)
    {
        int lastDigit = n % 10;
        sum = sum + (lastDigit * lastDigit * lastDigit);
        n = n / 10;
    }

    if (sum == original)
        cout << "Armstrong";
    else
        cout << "Not Armstrong";

    return 0;
}
06_Print_All_Divisors.cpp (Brute Force)
#include <iostream>
using namespace std;

int main()
{
    int n;
    cin >> n;

    for (int i = 1; i <= n; i++)
    {
        if (n % i == 0)
            cout << i << " ";
    }

    return 0;
}
06_Print_All_Divisors_Optimized.cpp
#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int n;
    cin >> n;

    for (int i = 1; i <= sqrt(n); i++)
    {
        if (n % i == 0)
        {
            cout << i << " ";

            if (n / i != i)
                cout << n / i << " ";
        }
    }

    return 0;
}
07_Prime_Number.cpp
#include <iostream>
using namespace std;

int main()
{
    int n;
    cin >> n;

    int count = 0;

    for (int i = 1; i <= n; i++)
    {
        if (n % i == 0)
            count++;
    }

    if (count == 2)
        cout << "Prime";
    else
        cout << "Not Prime";

    return 0;
}
08_GCD_or_HCF.cpp
#include <iostream>
using namespace std;

int main()
{
    int n1, n2;
    cin >> n1 >> n2;

    int gcd = 1;

    for (int i = 1; i <= min(n1, n2); i++)
    {
        if (n1 % i == 0 && n2 % i == 0)
            gcd = i;
    }

    cout << gcd;

    return 0;
}